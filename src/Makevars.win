PKG_CXXFLAGS += -DRCPP_PARALLEL_USE_TBB=1

PKG_LIBS += $(LAPACK_LIBS) $(BLAS_LIBS) $(FLIBS)
PKG_LIBS += $(shell "${R_HOME}/bin${R_ARCH_BIN}/Rscript.exe" \
              -e "RcppParallel::RcppParallelLibs()")

OBJECTS = bheap.o dgraph.o pathfinders.o run_sp.o

.PHONY: all clean

all: $(SHLIB) $(clean)

clean: $(SHLIB)
	rm -rf $(OBJECTS)
